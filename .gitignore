# ==================================
# ไฟล์นี้ชื่อ .gitignore
# ==================================

# Logs and Debug files
# ละเว้นไฟล์ Log ที่สร้างขึ้นระหว่างการทำงาน
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
# ละเว้นโฟลเดอร์ที่เก็บแพ็กเกจทั้งหมด (สำคัญมาก)
node_modules

# Build output
# ละเว้นโฟลเดอร์ผลลัพธ์จากการ build
dist
dist-ssr

# User-uploaded files
# ระบุ Path ไปยังโฟลเดอร์ uploads ที่อยู่ใน backend
/backend/uploads/

# Environment variables
# ละเว้นไฟล์ที่เก็บข้อมูลลับและตัวแปรเฉพาะเครื่อง (สำคัญมาก)
.env
.env.local
*.local

# Editor directories and files
# ละเว้นไฟล์ตั้งค่าส่วนตัวของโปรแกรม Editor
.vscode/*
!.vscode/extensions.json # แต่ให้แชร์ไฟล์ Extension ที่แนะนำ
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

playwright-report/
screenshots/
test-results/

# Node
node_modules/
dist/

# Playwright outputs
playwright-report/
test-results/
artifacts/
screenshots/

# Vite build cache
.vite/

# System
.DS_Store
Thumbs.db
